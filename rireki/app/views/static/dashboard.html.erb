Find your resume here: <%= link_to resume_url(user_id: current_user.id), resume_path(user_id: current_user.id) %>

<%= render partial: 'experiences/list' %>

<%= render partial: 'educations/list' %>

<%= render partial: 'activities/list', locals: { activities: @activities } %>

<%= render partial: 'qualifications/list' %>
